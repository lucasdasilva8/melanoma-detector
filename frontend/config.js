// Set API_URL to your deployed backend when hosting the frontend separately.
// For local development, this points to the FastAPI server on port 8000.
window.APP_CONFIG = {
  API_URL: "http://localhost:8000",
};
